# Database Configuration
# Local Development (with Docker Postgres)
DATABASE_URL="postgresql://trustuser:trustpass@localhost:5432/trustdb"

# Production (replace with your actual credentials)
# DATABASE_URL="postgresql://user:password@host:5432/database"

# API Keys
# Required for OpenAI function calling in chat
OPENAI_API_KEY=your_openai_api_key_here

# Optional - External Data Connectors
NEWSAPI_KEY=
TRUSTPILOT_API_KEY=
DATA_GOV_API_KEY=your_data_gov_api_key_here
OPENCORPORATES_KEY=
COURTLISTENER_API_KEY=your_courtlistener_api_key_here

# Application Configuration
API_KEY_MAIN=changeme
NODE_ENV=development
PORT=4000

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Redis Configuration (optional - uses in-memory cache if not configured)
# REDIS_URL=redis://localhost:6379

# Trust Score Configuration (optional)
# TRUST_INCLUDE_DIAGNOSTICS=false
